version: '3'

services:
  app:
    build: .

  db:
    build: db/.
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: sample_db
      MYSQL_ROOT_HOST: '%'
    ports:
      - "33060:3306"